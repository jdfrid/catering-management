# מסד מקומי (Docker) — מומלץ לפיתוח. הריצו: docker compose up -d
DATABASE_URL="postgresql://catering:catering@localhost:5433/catering_management?schema=public"

# אחסון ב-Render: העתיקו מ-Dashboard → Database → External Connection String
# DATABASE_URL="postgresql://..."

NEXT_PUBLIC_APP_NAME="מערכת ניהול קייטרינג"
EMAIL_FROM="quotes@example.co.il"
